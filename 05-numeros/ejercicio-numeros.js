const alturaCentimetros = 170;

const alturaMetros = 1.7;

const peso = 65.4;

const alturaRedondeo = Math.ceil(alturaMetros)
const pesoRedondeo = Math.floor(peso)

const valIguales = Number.MAX_VALUE + 1 == Number.MAX_VALUE

console.log(valIguales)